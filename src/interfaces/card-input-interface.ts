export interface CardInputInterface {
    cardNumber: string,
    cardSpacing: string,
    sheetRows: string,
    sheetColumns: string,
}

export interface CardInputInterfaceNumbers {
    cardNumber: number,
    cardSpacing: number,
    sheetRows: number,
    sheetColumns: number,
}